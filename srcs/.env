# Host login used to place named volumes under /home/${HOST_LOGIN}/data
HOST_LOGIN=gmiorcec

# Domain and TLS configuration
DOMAIN_NAME=gmiorcec.42.fr
CERT_PATH=/etc/ssl/certs/nginx-selfsigned.crt
KEY_PATH=/etc/ssl/private/nginx-selfsigned.key

# MariaDB configuration (non-secret)
DB_NAME=wp_db
DB_USER=wp_user

# WordPress configuration (non-secret)
WP_TITLE=InceptionSite
WP_ADMIN_USR=wpadminuser
WP_ADMIN_EMAIL=admin@example.com
WP_USER=wpuser
WP_USER_EMAIL=user@example.com

# Database host inside Docker network
DB_HOST=mariadb
